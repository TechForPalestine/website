---
import { getTestimonials } from "../data/testimonials";

/**
 * Legacy-design-system twin of `home/TestimonialsSection.astro`.
 *
 * The design-system version depends on `design-system.css` (Fraunces/Outfit and
 * the `ts-*` typography scale), which `Layout.astro` deliberately does not load.
 * Rather than pull that stylesheet into every legacy page, this renders the same
 * shared testimonial data in the legacy grey/green idiom.
 */
interface Props {
  membershipLive?: boolean;
  heading?: string;
  subheading?: string;
}

const {
  membershipLive = false,
  heading = "What it's like to work with T4P",
  subheading = "Project leaders, mentors, and members on their time inside the coalition.",
} = Astro.props;

const testimonials = getTestimonials(membershipLive);
---

<section class="border-t border-[#E4DFD3] bg-[#F2EEE4] py-16 font-sans">
  <div class="mx-auto max-w-6xl px-6 sm:px-8">
    <div class="mb-4 flex items-center gap-1" aria-hidden="true">
      <span class="h-[3px] w-4 rounded-full bg-[#168039]"></span>
      <span class="h-[3px] w-4 rounded-full bg-[#1B2420]"></span>
      <span class="h-[3px] w-4 rounded-full bg-[#B3272D]"></span>
    </div>
    <h2 class="font-serif text-3xl font-semibold tracking-tight text-[#1B2420]">{heading}</h2>
    <p class="mt-3 max-w-2xl text-lg text-[#6B7A70]">{subheading}</p>

    <div class="legacy-marquee -mx-6 mt-10 overflow-hidden sm:-mx-8" aria-label="Testimonials carousel">
      <div class="legacy-marquee-track flex w-max gap-6 px-6 will-change-transform sm:px-8">
        {
        [...testimonials, ...testimonials, ...testimonials].map((t, i) => (
          <figure
            class:list={[
              "legacy-testimonial-card flex min-w-[300px] max-w-[300px] shrink-0 flex-col rounded-[10px] border border-[#E4DFD3] bg-white p-6 sm:min-w-[400px] sm:max-w-[400px]",
              i >= testimonials.length && "legacy-marquee-dupe",
            ]}
          >
            <svg
              class:list={[
                "mb-5 shrink-0",
                ["text-[#168039]", "text-[#1B2420]", "text-[#B3272D]"][i % 3],
              ]}
              width="28"
              height="28"
              viewBox="0 0 24 24"
              fill="currentColor"
              aria-hidden="true"
            >
              <path d="M4.583 17.321C3.553 16.227 3 15 3 13.011c0-3.5 2.457-6.637 6.03-8.188l.893 1.378c-3.335 1.804-3.987 4.145-4.247 5.621.537-.278 1.24-.375 1.929-.311 1.804.167 3.226 1.648 3.226 3.489a3.5 3.5 0 01-3.5 3.5c-1.073 0-2.099-.49-2.748-1.179zm10 0C13.553 16.227 13 15 13 13.011c0-3.5 2.457-6.637 6.03-8.188l.893 1.378c-3.335 1.804-3.987 4.145-4.247 5.621.537-.278 1.24-.375 1.929-.311 1.804.167 3.226 1.648 3.226 3.489a3.5 3.5 0 01-3.5 3.5c-1.073 0-2.099-.49-2.748-1.179z" />
            </svg>
            <blockquote class="flex-1 text-base leading-relaxed text-[#3F4A43]">
              {t.quote}
            </blockquote>
            <figcaption class="mt-6 flex items-center gap-3">
              {t.headshots ? (
                <div class="relative h-12 w-12 shrink-0">
                  <img
                    src={t.headshots[0]}
                    alt=""
                    width="36"
                    height="36"
                    loading="lazy"
                    decoding="async"
                    class="absolute -left-2 top-0 z-10 h-8 w-8 rounded-full object-cover ring-1 ring-[#E4DFD3]"
                  />
                  <img
                    src={t.headshots[1]}
                    alt=""
                    width="36"
                    height="36"
                    loading="lazy"
                    decoding="async"
                    class="absolute bottom-0 right-0 h-8 w-8 rounded-full object-cover ring-1 ring-[#E4DFD3]"
                  />
                </div>
              ) : (
                <img
                  src={t.headshot}
                  alt=""
                  width="48"
                  height="48"
                  loading="lazy"
                  decoding="async"
                  class="h-12 w-12 rounded-full object-cover ring-1 ring-[#E4DFD3]"
                />
              )}
              <div>
                <p class="font-semibold text-[#1B2420]">{t.name}</p>
                <p class="text-sm text-[#6B7A70]">{t.role}</p>
              </div>
            </figcaption>
          </figure>
        ))
      }
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes legacy-marquee-scroll {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-33.333%);
    }
  }

  .legacy-marquee-track {
    animation: legacy-marquee-scroll 40s linear infinite;
  }

  .legacy-marquee:hover .legacy-marquee-track {
    animation-play-state: paused;
  }

  /* Without JS or with reduced motion, fall back to a static wrapped grid. */
  :global(html.no-js) .legacy-marquee-dupe {
    display: none;
  }
  :global(html.no-js) .legacy-marquee {
    overflow: visible;
  }
  :global(html.no-js) .legacy-marquee-track {
    width: 100%;
    flex-wrap: wrap;
    justify-content: center;
    animation: none;
  }
  :global(html.no-js) .legacy-testimonial-card {
    flex: 1 1 300px;
  }

  @media (prefers-reduced-motion: reduce) {
    .legacy-marquee-dupe {
      display: none;
    }
    .legacy-marquee {
      overflow: visible;
    }
    .legacy-marquee-track {
      width: 100%;
      flex-wrap: wrap;
      justify-content: center;
      animation: none;
    }
    .legacy-testimonial-card {
      flex: 1 1 300px;
    }
  }
</style>
